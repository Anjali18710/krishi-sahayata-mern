# Only needed when the frontend and backend are deployed separately
# (e.g. frontend on Vercel, backend on Render). Leave empty for local development.
# Example: VITE_API_URL=https://krishi-sahayata-api.onrender.com/api
VITE_API_URL=
